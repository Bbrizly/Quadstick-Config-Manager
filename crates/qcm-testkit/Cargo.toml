[package]
name = "qcm-testkit"
version = "0.1.0"
description = "Deterministic fakes for QuadStick core ports"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[lints]
workspace = true

[dependencies]
qcm-core = { path = "../qcm-core" }
